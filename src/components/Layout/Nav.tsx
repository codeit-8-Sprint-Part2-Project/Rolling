import React from "react";
import NavLogo from "../UI/NavLogo";
import NavContainer from "../UI/NavContainer";

const Nav: React.FC = () => {
  return (
    <NavContainer>
      <NavLogo />
    </NavContainer>
  );
};

export default Nav;
